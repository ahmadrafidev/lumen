import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export default function PassCheckCard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-center">Pass Check</CardTitle>
      </CardHeader>
      <CardContent>
        {/* Pass check logic will be display here */}
      </CardContent>
    </Card>
  );
}